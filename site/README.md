# INAVTO ASIA — сайт

Маркетинговый сайт «Авто из Китая под заказ с доставкой под ключ в Россию».
Статический (чистые HTML/CSS/JS, без сборки) — можно открыть локально или разместить на любом хостинге.

> Продукт — **только Китай**. Корея из данных, страниц, текстов и генератора удалена и не возвращается.

## Страницы

| Файл | Назначение |
|---|---|
| `index.html` | Главная: оффер, hero с фото, витрина каталога, трекер, этапы, FAQ |
| `catalog.html` | Каталог новых авто из Китая с фильтрами (кузов, двигатель, бюджет) |
| `cars/<slug>.html` | Статические SEO-страницы моделей (16 шт., Schema.org Product + BreadcrumbList + FAQ) |
| `car.html?slug=...` | Динамическая карточка (fallback, закрыта в robots.txt) |
| `calculator.html` | Калькулятор цены «под ключ» (пошлина, утильсбор, логистика, комиссия) |
| `kak-my-rabotaem.html` | Этапы заказа + трекер |
| `garantii.html` | Гарантии сделки |
| `dostavka.html` | Доставка и растаможка |
| `dlya-biznesa.html` | Условия для юрлиц и перекупщиков (партии) |
| `vydannye-avto.html` | Выданные авто: фото вручений (данные — `INAVTO.DELIVERED` в data.js) |
| `o-kompanii.html` | О компании |
| `kontakty.html` | Контакты + обратный звонок |
| `privacy.html` | Политика конфиденциальности |

## Дизайн-система (v3.1 «Editorial Red»)

- `css/style.css` — премиальный минимализм: бумага `#FAFAFA` (фон), чернила `#050505` (текст, линии), фирменный красный `#C8102E` (`--red`) — кнопки, акценты заголовков, лейблы, цены, трекер, квиз; `border-radius: 0`, линии 1px, бенто-сетки через `gap:1px`.
- Типографика: **Unbounded** (display, UPPERCASE-заголовки) + **Inter** (текст). У `.h1 .accent` — прозрачная заливка с обводкой.
- Кастомный курсор-точка, инверсия кнопок при ховере, параллакс hero-фото, reveal-анимации, `prefers-reduced-motion` уважается.
- `js/data.js` — каталог (16 моделей) и контакты (`INAVTO.CONTACTS`; телефон и Telegram реальные, e-mail — заглушка до подтверждения).
- `js/main.js` — инжект шапки/футера/квиза/мобильного CTA-бара на все страницы, ветвящийся квиз (с выбором способа связи), формы с маской телефона, аналитика, курсор, параллакс, переключатель языка.
- `js/i18n.js` — мультиязычность Рус / 中文 / EN (английский): словарь + паттерны, перевод DOM на лету (MutationObserver), выбор хранится в `localStorage('inavto_lang')`. Исходный контент — русский; при изменении текстов страниц нужно дополнять словарь.

## Генерация SEO-страниц

После любого изменения каталога в `js/data.js`:

```bash
node build-pages.mjs   # пересоздаёт cars/*.html, sitemap.xml, robots.txt
```

Перед публикацией замените `DOMAIN` в `build-pages.mjs` на реальный домен.

## Аналитика

Вставьте счётчик Яндекс.Метрики в `<head>` всех страниц и задайте
`window.INAVTO_YM_ID = XXXXXXXX` до подключения `main.js`. Цели отправляются
автоматически через `INAVTO.goal(...)`: `lead_submit`, `quiz_open`,
`quiz_step_N`, `calc_used`, `phone_click`, `tg_click`, `wa_click`, `form_abandon`.

## Заявки

Все формы отправляют POST на `/api/selections` — существующий backend из `backend/`
(заявка приходит менеджеру в Telegram-бот). Если сайт на другом домене,
задайте адрес API до подключения `main.js`:

```html
<script>window.INAVTO_API = 'https://your-backend.example.com'</script>
```

Если API недоступен (чисто статический хостинг), форма всё равно покажет
подтверждение и предложит написать в Telegram.

## Что дальше

Дорожная карта — в `docs/TZ-GLOBAL-IMPROVEMENT.md` (этапы A/B/C).
Ждут материалов владельца: реальные контакты, домен, номер счётчика Метрики,
фото моделей (в `img/` по слагам), фото вручений для блока «Выданные авто».
